** Here is the question, database type, database schema, directives, SQL to evaluate, and the unit tests to apply: **

**Question**
{QUESTION}

---
**Database type**
{DATABASE_TYPE}

---
**Schema**
{DATABASE_SCHEMA}

---
**Directives**
{DIRECTIVES}

---
**Evidence**
{EVIDENCE}

*** SQL Query to Evaluate: ***
{SQL_QUERY}

*** Unit Tests to Apply: ***
{UNIT_TESTS}

*** Gold SQL Examples (Reference Only - Do NOT Evaluate These): ***
{GOLD_SQL_EXAMPLES}

You are a professional tester specialized in evaluation of SQL statements to answer questions expressed in natural language.

Your task is to:
1. Read the SQL query and all the information provided (question, database type and schema, directives, evidence)
2. Use the Gold SQL examples as reference to understand expected query patterns (these are NOT for evaluation)
3. Apply EACH test to the SQL query, evaluating each test individually

For your thinking field:
- Keep it brief. You can leave it empty or provide a very short summary if needed.

For your answers field:
Provide a LIST of strings - one string for EACH test.

Each string in the list must be formatted as:
"Test #{number}: {result}"

Where result is EXACTLY one of:
- "OK" if the SQL passes that specific test
- "KO - {brief_reason}" if the SQL fails that specific test (provide a brief explanation)

Example answers list for 3 tests:
[
  "Test #1: OK",
  "Test #2: OK", 
  "Test #3: KO - missing WHERE clause for charter schools"
]

CRITICAL: You MUST provide exactly as many answer strings as there are tests - one string for each test.

IMPORTANT: Evaluate EACH test against the SQL individually and report the result for every test.